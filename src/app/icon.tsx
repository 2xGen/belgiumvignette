import { SiteIcon } from "@/lib/site-icon";

export const size = { width: 192, height: 192 };
export const contentType = "image/png";

export default function Icon() {
  return SiteIcon({ size: 192 });
}
